<?xml version="1.0" encoding="UTF-8"?>
 
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd
       ">
  
    <camelContext xmlns="http://camel.apache.org/schema/spring">
    <!-- >threadPoolProfile id="sharedNIOPool" poolSize="2" maxPoolSize="2" defaultProfile="true" / -->
		
        <route >
            <from uri="direct:sampleroute1" />
            <setExchangePattern pattern="InOut" />
            <to uri="bean:myBean?method=computeText"/>
			<multicast id="searchAndBrowseWithStoreIds" parallelProcessing="true" stopOnException="true" executorServiceRef="sharedNIOPool">
				<pipeline>
					<to uri="bean:myBean?method=appendSomeText1"/>
					<to uri="bean:myBean?method=appendSomeText2"/>
				</pipeline>
				<pipeline>
					<to uri="bean:myBean?method=appendSomeText3"/>
					<to uri="bean:myBean?method=appendSomeText4"/>
					<to uri="bean:myBean?method=appendSomeText1"/>
					<to uri="bean:myBean?method=appendSomeText2"/>
				</pipeline>
			</multicast>
        </route>  
        
        <route>
            <from uri="direct:sampleroute2" />
            <setExchangePattern pattern="InOut" />
            <to uri="bean:myBean?method=appendSomeText2"/>
        </route> 
        
        <route >
            <from uri="direct:sampleroute3" />
            <setExchangePattern pattern="InOut" />
            <to uri="bean:myBean?method=appendSomeText2"/>
			<multicast id="searchAndBrowse2" parallelProcessing="true" stopOnException="true" executorServiceRef="sharedNIOPool">
						<to uri="bean:myBean?method=appendSomeText3"/>
						<to uri="bean:myBean?method=appendSomeText4"/>
						<to uri="bean:myBean?method=appendSomeText1"/>
					    <to uri="bean:myBean?method=appendSomeText2"/>
			</multicast>
        </route>
        
    </camelContext>
    
    <bean id="myBean" class="CamelBeanEvent"/>
    
    <bean id="poolBuilder" class="org.apache.camel.component.netty4.NettyWorkerPoolBuilder">
                <property name="workerCount" value="4"/>
    </bean>
              
              
    <bean id="sharedNIOPool" class="io.netty.channel.EventLoopGroup"
                factory-bean="poolBuilder" factory-method="build" destroy-method="shutdown">
    </bean>
 
</beans>

<?xml version="1.0" encoding="UTF-8"?>
 
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xs/
        http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd
       ">
    <camelContext xmlns="http://camel.apache.org/schema/spring">
    <threadPoolProfile id="threadprofile" poolSize="10" maxPoolSize="20" defaultProfile="true" />
		
        <route >
            <from uri="direct:sampleroute1" />
            <setExchangePattern pattern="InOut" />
            <to uri="bean:myBean?method=appendSomeText5"/>
            
					<multicast id="testId" parallelProcessing="true" stopOnException="true" executorServiceRef="threadprofile">
						<to uri="bean:myBean?method=appendSomeText2"/>
						<to uri="bean:myBean?method=appendSomeText2"/>
					</multicast>

        </route>
    </camelContext>
    <bean id="myBean" class="CamelBean"/>
 
</beans>
